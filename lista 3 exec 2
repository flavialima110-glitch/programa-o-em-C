#include <stdio.h>

int main() {
    int tipoUsuario, horario, autorizacao;

    printf("Digite o tipo de usuario (1-aluno, 2-professor): ");
    scanf("%d", &tipoUsuario);

    printf("Digite o horario atual: ");
    scanf("%d", &horario);

    printf("Digite a autorizacao especial (1/0): ");
    scanf("%d", &autorizacao);

    // if ( 'Se tipoUsuario == 2 ') {
    printf ('&entrada permitida');
}
    if ('Se tipoUsuario == 1:') {
    printf ('entre 8 && 18 →  entrada permitida ')
}
   if ('fora desse horário → só enta se autorizacao == 1') {
    printf('Caso contrário, &entrada negada');
    }
    return 0;
}
